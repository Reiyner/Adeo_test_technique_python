# Run the project 
This project can be launched by installing and running poetry

first make sure you use the following command line:
    poetry install

## Tests

To run tests you must use the following command line after installing the poetry environment :

    poetry run pytest -v